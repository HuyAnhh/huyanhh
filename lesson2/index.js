let arr = [60 , 40 , 55 , 75 , 64]

for(let i=0 ; i<arr.length ; i+2) {
    let sum = 0;
    sum+= arr[i]
    console.log(sum)
}

for(let i=1 ; i<arr.length ; i+2) {
    let sum2=0;
    sum2+= arr[i]
    console.log(sum2)
}